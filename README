Android app to use while doing the Bridge to 10K program. Currently, the app only shows a list of all workouts with a detail view page for each workout. There is no way to mark workouts as done or (more importantly) no way to use the app while doing a workout.
